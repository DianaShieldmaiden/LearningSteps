def computepay(h,r):

    if h<40:
        pay=h*r
        print pay
    else:
        pay=40*r+(h-40)*1.5*r
        print pay
        
        return pay

hrs = raw_input("Enter Hours:")
rate = raw_input("Enter rate:")
h= float(hrs)
r= float(rate)

p = computepay(h,r)

