hrs = raw_input("Enter Hours:")
rate = raw_input("Enter rate:")
h = float(hrs)
r = float(rate)

if hrs <= 40 :
      pay = h*r
      print pay

elif hrs > 40 :
      hp = 40
      he = (h-40)
      nr = r * 1.5  
      pay = hp * r + he * nr
      print pay
